\documentclass{standalone}

\usepackage{pgfplots}
\usepgfplotslibrary{statistics}

\begin{document}
  \begin{tikzpicture}
    \begin{axis}[title={Steal operations},              % Title of the graph
                 xtick={1,2,4,8,16,32,64},  % The ticks on the x-axis
                 xlabel={number of threads},        % Label of the x-axis
                 ylabel={number of calls to TryStealBlock},             % Label of the y-axis
                 legend style={
                   at={(1.05,0.95)},                % Position of the legend anchor
                   anchor=north west                % The legend anchor
                 }]

      % Each graph is added via \addplot and a .data file, and
      % add a legend entry for the previous graph.
      \addplot table [x=x,y=num_Steal]{\DATAPATH/bench_add_remove_10000/bench_add_remove_10000.data};
      \addlegendentry{add remove}

      \addplot table [x=x,y=num_Steal]{\DATAPATH/bench_half_half_10000/bench_half_half_10000.data};
      \addlegendentry{half half}

      \addplot table [x=x,y=num_Steal]{\DATAPATH/bench_one_consumer_10000/bench_one_consumer_10000.data};
      \addlegendentry{one consumer}

      \addplot table [x=x,y=num_Steal]{\DATAPATH/bench_one_producer_10000/bench_one_producer_10000.data};
      \addlegendentry{one producer}

      \addplot table [x=x,y=num_Steal]{\DATAPATH/benchrand_10000/benchrand_10000.data};
      \addlegendentry{random}

    \end{axis}
  \end{tikzpicture}
\end{document}
