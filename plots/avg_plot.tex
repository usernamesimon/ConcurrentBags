\documentclass{standalone}

\usepackage{pgfplots}
\usepgfplotslibrary{statistics}

\begin{document}
  \begin{tikzpicture}
    \begin{axis}[title={Parallel sum},              % Title of the graph
                 xtick={1,2,4,8},  % The ticks on the x-axis
                 xlabel={number of threads},        % Label of the x-axis
                 ylabel={time in [ms]},             % Label of the y-axis
                 legend style={
                   at={(1.05,0.95)},                % Position of the legend anchor
                   anchor=north west                % The legend anchor
                 }]

      % Each graph is added via \addplot and a .data file, and
      % add a legend entry for the previous graph.
      \addplot table [x=x,y=avg_time]{\DATAPATH/avg/smallbench_100.data};
      \addlegendentry{100 elements/thread}

      \addplot table [x=x,y=avg_time]{\DATAPATH/avg/smallbench_1000.data};
      \addlegendentry{1000 elements/thread}

    \end{axis}
  \end{tikzpicture}

  \begin{tikzpicture}
    \begin{axis}[title={Parallel sum},              % Title of the graph
                 xtick={1,2,4,8},  % The ticks on the x-axis
                 xlabel={number of threads},        % Label of the x-axis
                 ylabel={time in [ms]},             % Label of the y-axis
                 legend style={
                   at={(1.05,0.95)},                % Position of the legend anchor
                   anchor=north west                % The legend anchor
                 }]

      % Each graph is added via \addplot and a .data file, and
      % add a legend entry for the previous graph.
      \addplot table [x=x,y=throughput]{\DATAPATH/avg/smallbench_100.data};
      \addlegendentry{100 elements/thread}

      \addplot table [x=x,y=throughput]{\DATAPATH/avg/smallbench_1000.data};
      \addlegendentry{1000 elements/thread}

    \end{axis}
\end{tikzpicture}
\end{document}
